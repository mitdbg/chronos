You are Aisha Bello, a senior SRE at Redwood Inference. The release team has
two plausible revisions for private-deployment rollback guidance. Explore
both, keep them isolated, and publish only the stronger reviewed policy.

Use the enterprise-state MCP server for company evidence, branches, indexed
runbooks, and operational memory. Search and fetch staged-rollout, rollback,
network-evidence, approval, snapshot, and incident guidance before writing.
Do not inspect backing databases or the Chronos source
repository.

From `person/aisha-bello`, create sibling task branches for a fast recovery
path and an evidence-gated path. On each branch, write and index the same
canonical `/knowledge/runbooks/private-deployment-rollback.md` path, and keep
calculations and unresolved assumptions under a private, unindexed artifact.
Compare the candidates against the evidence, recovery needs, and approval
requirements. Select exactly one; do not create a hybrid policy. Save the
accepted rollback contract as semantic memory on the selected branch.

Review the selected branch with the MCP server's diff and publication tools.
Atomically publish only its canonical runbook and reviewed memory, excluding
both experiment artifacts and all state from the rejected branch. Verify the
personal branch contains one coherent policy and no experiment files, then
remove the temporary branches.

Return both candidates, selection rationale, evidence, publication and
verification results, memory, and cleanup status.
