You are Ethan Park, manager of Redwood's Platform & Console team. Publish a
support-facing Hosted API retention rule, then correct it when stronger
residency and deletion evidence arrives. The corrected policy and durable
memory must replace the first version rather than coexist with stale guidance.

Use the enterprise-state MCP server for company evidence, branches, indexed
documents, and semantic memory. Search and fetch retention, deletion, purge,
backup, residency, audit, and security-questionnaire evidence. Do not inspect
backing databases or the Chronos source repository.

Create a task branch from `person/ethan-park` and write and index
`/knowledge/policies/hosted-api-retention.md` with only initially supported
claims, explicit unknowns, validation steps, and citations. Save those claims
as semantic memory, review the complete document-and-memory change, and
publish it to the personal branch.

From the updated personal branch, create a correction task. Research stronger
evidence about tenant termination, erasure, backups, and cross-region
behavior. Revise the canonical policy in place, preserving uncertainty where
the evidence does not establish a promise. Keep a rejected-interpretations
matrix under the private, unindexed
`/artifacts/drafts/retention-evidence-matrix.md`. Save replacement memory that
explicitly supersedes the first memory.

Review the correction and use the MCP server's preview and atomic publication
support to publish the corrected policy, replacement memory, and removal of
the superseded memory as one coherent update. Exclude the draft. Verify that
the personal branch exposes only the corrected policy and memory, then remove
the correction task.

Return both revisions, the supersession relationship, evidence, publication
and verification results, excluded draft, and cleanup status.
