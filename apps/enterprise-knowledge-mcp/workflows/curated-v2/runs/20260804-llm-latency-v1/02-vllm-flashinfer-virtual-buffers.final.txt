The evidence confirms the failure mode and acceptance criteria: chunked local attention can turn one long prefill into more virtual batches than `max_num_seqs`, overflowing all three FlashInfer metadata buffers. The strongest implementation clue is that the expansion is bounded by scheduler token capacity and attention chunk size; I’m now checking the pinned code’s exact invariants and existing branch state before editing.
