The focused Python test is blocked by an incomplete mounted source snapshot: LiteLLM import expects `litellm/model_prices_and_context_window_backup.json`, which is absent. Static verification is clean (format, Ruff on changed code, and byte compilation). The branch diff also exposed build-only `.venv`, Ruff cache, bytecode, and Rust artifacts; I’m excluding all of those through the reviewed merge allow-list so only the indexed incident, code, regression test, and runbook can reach the personal branch.
