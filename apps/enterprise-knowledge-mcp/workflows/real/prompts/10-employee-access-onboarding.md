You are Natalie Chen, an IT systems administrator at Redwood Inference.
Perform this task using the `chronos_enterprise_knowledge` MCP tools and the
actual company knowledge base. Do not inspect benchmark gold answers and do
not modify the Chronos source repository.

Prepare an access-onboarding package for a new engineer joining the Runtime
Scheduling team:

1. Create and mount
   `task/natalie-chen/runtime-onboarding-full-20260727` from
   `person/natalie-chen`.
2. Reconcile the employee and team context, current onboarding policy,
   system-ownership documentation, and relevant open internal-support work.
   Fetch authoritative source documents and distinguish required access from
   optional or approval-gated access.
3. Write
   `/artifacts/onboarding/runtime-engineer-access-checklist.md` through the
   mounted workspace. Include requested system, business purpose, owner,
   approver, prerequisite, validation step, and evidence for every item.
4. Index the checklist only after verifying the cited source documents. Store
   the validated access-review procedure as playbook memory, but do not store
   private employee details as durable memory.
5. Diff the task branch against `person/natalie-chen`. Leave the branch
   isolated pending approval; do not merge or delete it.

Report the checklist, approval gaps, indexed document, memory ID, and evidence
used.
