[tool.uv.workspace]
members = [
  "packages/chronos-core",
  "packages/chronos-langchain",
  "packages/chronos-langgraph",
  "packages/chronos-code",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
