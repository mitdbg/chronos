[tool.uv.workspace]
members = [
  "packages/chronos-core",
  "packages/chronos-langchain",
  "packages/chronos-langgraph",
  "packages/chronos-code",
  "apps/enterprise-knowledge-mcp",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
markers = [
  "chronos_lake: requires a local S3-compatible service",
]
