SPECIFICATION Spec

CONSTRAINT StateBound

INVARIANTS
  TypeOK
  SegmentAncestryOK
  NoOverlappingPhysicalRowsPerKey
  AtMostOneVisibleRow
  PhysicalRowsMatchBranchView
  WriterSegmentKnown
  WriterIntervalContainsRow
  VisibleWriterOnReaderPath

CONSTANTS
  Root = main
  RootSegment = s0
  Nil = nil
  BranchIds = {main, b1, b2}
  SegmentIds = {s0, s1, s2, s3, s4}
  Keys = {k1, k2}
  Values = {v1, v2}
  MaxInterval = 16
