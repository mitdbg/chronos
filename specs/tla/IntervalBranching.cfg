SPECIFICATION Spec

CONSTRAINT StateBound

INVARIANTS
  TypeOK
  NoOverlappingPhysicalRowsPerKey
  AtMostOneVisibleRow
  PhysicalRowsMatchBranchView

CONSTANTS
  Root = main
  Nil = nil
  BranchIds = {main, b1, b2}
  Keys = {k1, k2}
  Values = {v1, v2}
  MaxInterval = 16
